#include <iostream>
int main() {
    setlocale(LC_CTYPE, "Russian");
    int a, b, p, q, r, s;
    std::cout << "Введите длинну участка a: ";
    std::cin >> a;
    std::cout << "Введите ширену дома b: ";
    std::cin >> b;
    std::cout << "Введите значение переменной p: ";
    std::cin >> p;
    k = (m % 5 == 0) ? (k + m) : (k + 0);
    k = (n % 5 == 0) ? (k + n) : (k + 0);
    k = (p % 5 == 0) ? (k + p) : (k + 0);

    std::cout << "Сумма чисел которые делятся на 5 = " << k << std::endl;
    return 0;
}
